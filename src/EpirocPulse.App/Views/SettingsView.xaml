<Page x:Class="EpirocPulse.App.Views.SettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Settings"
      Background="{StaticResource BackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel>
            <!-- Header -->
            <TextBlock FontSize="28" FontWeight="Bold" Text="Settings" Margin="0,0,0,16"/>
            <TextBlock FontSize="14" Foreground="{StaticResource TextLightBrush}" Text="Configure application preferences" Margin="0,0,0,24"/>

            <!-- General Settings -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock FontSize="16" FontWeight="SemiBold" Text="General Settings" Margin="0,0,0,16"/>

                    <!-- Setting 1 -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock FontSize="12" FontWeight="SemiBold" Text="Diagnostic Timeout (seconds)"/>
                            <TextBlock FontSize="11" Foreground="{StaticResource TextLightBrush}" Text="Maximum time to wait for diagnostic completion" Margin="0,4,0,0"/>
                        </StackPanel>
                        <TextBox Grid.Column="1" Width="100" Padding="8,4,8,4" Text="30" FontSize="12"/>
                    </Grid>

                    <Separator Background="#E0E0E0" Margin="0,0,0,16"/>

                    <!-- Setting 2 -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock FontSize="12" FontWeight="SemiBold" Text="Auto-run Diagnostics on Startup"/>
                            <TextBlock FontSize="11" Foreground="{StaticResource TextLightBrush}" Text="Run basic diagnostics when the application starts" Margin="0,4,0,0"/>
                        </StackPanel>
                        <CheckBox Grid.Column="1" IsChecked="False" VerticalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Machine Configuration -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock FontSize="16" FontWeight="SemiBold" Text="Machine Configuration" Margin="0,0,0,16"/>

                    <!-- Machine IP -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" FontSize="12" FontWeight="SemiBold" Text="Expected Machine IP:"/>
                        <TextBox Grid.Column="1" Padding="8,4,8,4" Text="192.168.1.100" FontSize="12" Margin="16,0,0,0"/>
                    </Grid>

                    <!-- Machine Port -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" FontSize="12" FontWeight="SemiBold" Text="Expected Port:"/>
                        <TextBox Grid.Column="1" Padding="8,4,8,4" Text="8080" FontSize="12" Margin="16,0,0,0"/>
                    </Grid>

                    <!-- Save Button -->
                    <Button Padding="12,8,12,8" Width="120" Background="{StaticResource AccentBrush}" Foreground="White" Content="Save Settings" FontSize="12" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>

            <!-- About Section -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock FontSize="16" FontWeight="SemiBold" Text="About Epiroc Pulse" Margin="0,0,0,12"/>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" FontSize="12" Text="Version:" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="1" FontSize="12" Text="1.0.0-alpha"/>
                    </Grid>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" FontSize="12" Text="Build:" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="1" FontSize="12" Text="Build date - 2026"/>
                    </Grid>
                    <TextBlock FontSize="11" Foreground="{StaticResource TextLightBrush}" TextWrapping="Wrap" Text="Epiroc Pulse is a network diagnostic tool for Epiroc field technicians. For support, contact your administrator."/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
