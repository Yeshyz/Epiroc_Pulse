<Page x:Class="EpirocPulse.App.Views.ReportsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Reports"
      Background="{StaticResource BackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel>
            <!-- Header -->
            <TextBlock FontSize="28" FontWeight="Bold" Text="Reports" Margin="0,0,0,16"/>
            <TextBlock FontSize="14" Foreground="{StaticResource TextLightBrush}" Text="View and export diagnostic reports" Margin="0,0,0,24"/>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                <Button Padding="12,8,12,8" Background="{StaticResource AccentBrush}" Foreground="White" Content="Generate Report" FontSize="13" FontWeight="SemiBold" Margin="0,0,8,0"/>
                <Button Padding="12,8,12,8" Background="Transparent" Foreground="{StaticResource TextLightBrush}" BorderBrush="{StaticResource TextLightBrush}" BorderThickness="1" Content="Export as PDF" FontSize="13"/>
            </StackPanel>

            <!-- Recent Reports -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock FontSize="16" FontWeight="SemiBold" Text="Recent Reports" Margin="0,0,0,12"/>
                    <TextBlock FontSize="12" Foreground="{StaticResource TextLightBrush}" Text="No reports generated yet. Run a diagnostic or generate a new report to get started." Margin="0,0,0,12"/>

                    <Button Padding="12,8,12,8" Background="{StaticResource AccentBrush}" Foreground="White" Content="Create First Report" FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- Report Templates -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock FontSize="16" FontWeight="SemiBold" Text="Available Templates" Margin="0,0,0,16"/>
                    
                    <!-- Template 1 -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock FontSize="13" FontWeight="SemiBold" Text="Quick Diagnostic Summary"/>
                            <TextBlock FontSize="11" Foreground="{StaticResource TextLightBrush}" Text="One-page overview of network status" Margin="0,4,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="1" Padding="8,4,8,4" Background="{StaticResource AccentBrush}" Foreground="White" Content="Use Template" FontSize="11"/>
                    </Grid>

                    <Separator Background="#E0E0E0" Margin="0,0,0,12"/>

                    <!-- Template 2 -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock FontSize="13" FontWeight="SemiBold" Text="Detailed Analysis"/>
                            <TextBlock FontSize="11" Foreground="{StaticResource TextLightBrush}" Text="Complete technical report with recommendations" Margin="0,4,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="1" Padding="8,4,8,4" Background="{StaticResource AccentBrush}" Foreground="White" Content="Use Template" FontSize="11"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
