<Page x:Class="EpirocPulse.App.Views.DashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Dashboard"
      Background="{StaticResource BackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel>
            <!-- Welcome Section -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock FontSize="24" FontWeight="Bold" Text="Welcome to Epiroc Pulse"/>
                    <TextBlock FontSize="14" Foreground="{StaticResource TextLightBrush}" Text="Network diagnostics and troubleshooting for Epiroc machines" Margin="0,8,0,0"/>
                    <TextBlock FontSize="12" Foreground="{StaticResource TextLightBrush}" Margin="0,8,0,0" Text="Ready to diagnose network problems? Use the menu to the left to get started."/>
                </StackPanel>
            </Border>

            <!-- Application Status Section -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock FontSize="16" FontWeight="SemiBold" Text="Application Status" Margin="0,0,0,12"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Status Item 1 -->
                        <StackPanel Grid.Column="0">
                            <TextBlock FontSize="12" Foreground="{StaticResource TextLightBrush}" Text="Application"/>
                            <Border Style="{StaticResource PassBadgeStyle}" Margin="0,8,0,0">
                                <TextBlock Style="{StaticResource BadgeTextStyle}" Text="Ready"/>
                            </Border>
                        </StackPanel>

                        <!-- Status Item 2 -->
                        <StackPanel Grid.Column="1">
                            <TextBlock FontSize="12" Foreground="{StaticResource TextLightBrush}" Text="Network Access"/>
                            <Border Style="{StaticResource InfoBadgeStyle}" Margin="0,8,0,0">
                                <TextBlock Style="{StaticResource BadgeTextStyle}" Text="Checking"/>
                            </Border>
                        </StackPanel>

                        <!-- Status Item 3 -->
                        <StackPanel Grid.Column="2">
                            <TextBlock FontSize="12" Foreground="{StaticResource TextLightBrush}" Text="Diagnostics"/>
                            <Border Style="{StaticResource SkippedBadgeStyle}" Margin="0,8,0,0">
                                <TextBlock Style="{StaticResource BadgeTextStyle}" Text="Not Run"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Diagnostic Cards Section -->
            <TextBlock FontSize="16" FontWeight="SemiBold" Text="Available Diagnostics" Margin="0,0,0,16"/>

            <!-- Network Adapter Card -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" FontSize="14" FontWeight="SemiBold" Text="Network Adapters"/>
                        <Border Grid.Column="1" Style="{StaticResource PassBadgeStyle}">
                            <TextBlock Style="{StaticResource BadgeTextStyle}" Text="Pass"/>
                        </Border>
                    </Grid>

                    <TextBlock Grid.Row="1" FontSize="12" Foreground="{StaticResource TextLightBrush}" Text="Detected active network adapters" Margin="0,12,0,0"/>
                    <TextBlock Grid.Row="2" FontSize="11" Foreground="{StaticResource TextLightBrush}" Text="Technical details: 2 adapters detected, 1 active" Margin="0,8,0,0"/>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Padding="8,4,8,4" Background="{StaticResource AccentBrush}" Foreground="White" Content="Run Diagnostic" FontSize="11" Margin="0,0,8,0"/>
                        <Button Padding="8,4,8,4" Background="Transparent" Foreground="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" Content="View Details" FontSize="11"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Gateway Reachability Card -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" FontSize="14" FontWeight="SemiBold" Text="Gateway Reachability"/>
                        <Border Grid.Column="1" Style="{StaticResource WarningBadgeStyle}">
                            <TextBlock Style="{StaticResource BadgeTextStyle}" Text="Warning"/>
                        </Border>
                    </Grid>

                    <TextBlock Grid.Row="1" FontSize="12" Foreground="{StaticResource TextLightBrush}" Text="Check if the network gateway is accessible" Margin="0,12,0,0"/>
                    <TextBlock Grid.Row="2" FontSize="11" Foreground="{StaticResource TextLightBrush}" Text="Technical details: Gateway responds to ping but response time is high" Margin="0,8,0,0"/>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Padding="8,4,8,4" Background="{StaticResource AccentBrush}" Foreground="White" Content="Run Diagnostic" FontSize="11" Margin="0,0,8,0"/>
                        <Button Padding="8,4,8,4" Background="Transparent" Foreground="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" Content="View Details" FontSize="11"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Machine Connection Card -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" FontSize="14" FontWeight="SemiBold" Text="Machine Connection"/>
                        <Border Grid.Column="1" Style="{StaticResource FailBadgeStyle}">
                            <TextBlock Style="{StaticResource BadgeTextStyle}" Text="Fail"/>
                        </Border>
                    </Grid>

                    <TextBlock Grid.Row="1" FontSize="12" Foreground="{StaticResource TextLightBrush}" Text="Verify connection to the Epiroc machine" Margin="0,12,0,0"/>
                    <TextBlock Grid.Row="2" FontSize="11" Foreground="{StaticResource TextLightBrush}" Text="Technical details: Machine not responding on expected port" Margin="0,8,0,0"/>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Padding="8,4,8,4" Background="{StaticResource AccentBrush}" Foreground="White" Content="Run Diagnostic" FontSize="11" Margin="0,0,8,0"/>
                        <Button Padding="8,4,8,4" Background="Transparent" Foreground="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" Content="View Details" FontSize="11"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Recent Activity Section -->
            <TextBlock FontSize="16" FontWeight="SemiBold" Text="Recent Activity" Margin="0,0,0,16"/>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <!-- Activity Item 1 -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource PassBadgeStyle}">
                            <TextBlock Style="{StaticResource BadgeTextStyle}" Text="✓"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                            <TextBlock FontSize="12" Text="Network Adapters diagnostic completed"/>
                            <TextBlock FontSize="11" Foreground="{StaticResource TextLightBrush}" Text="All adapters detected successfully" Margin="0,4,0,0"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" FontSize="11" Foreground="{StaticResource TextLightBrush}" Text="2 minutes ago"/>
                    </Grid>

                    <Separator Background="#E0E0E0" Margin="0,12,0,12"/>

                    <!-- Activity Item 2 -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource InfoBadgeStyle}">
                            <TextBlock Style="{StaticResource BadgeTextStyle}" Text="ℹ"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                            <TextBlock FontSize="12" Text="Application started"/>
                            <TextBlock FontSize="11" Foreground="{StaticResource TextLightBrush}" Text="Epiroc Pulse is ready to use" Margin="0,4,0,0"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" FontSize="11" Foreground="{StaticResource TextLightBrush}" Text="15 minutes ago"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
